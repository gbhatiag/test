# Python Project Structure

This repository contains a sample Python project structure with `src` and `test` folders.

- The `src` folder contains the main application code.
- The `test` folder contains test code.
- A sample Python file `src/hello.py` demonstrates a simple function.
